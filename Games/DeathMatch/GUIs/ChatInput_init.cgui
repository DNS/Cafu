-- This is a Cafu engine GUI script file, written by CaWE, the Cafu World Editor.
-- You CAN edit this file manually, but note that CaWE may overwrite your changes.
-- It is recommended that you place all your customizations like method overrides
-- and effects into a separate .cgui file that calls dofile() for including this.


-- Instantiation of all windows.
-- *****************************

Background=gui:new("WindowT", "Background");

ChatInputFrame=gui:new("WindowT", "ChatInputFrame");
ChatInputFrame.ChatInput=gui:new("WindowT", "ChatInput");
ChatInputFrame.ButtonCancel=gui:new("WindowT", "ButtonCancel");


-- Set the GUIs root window.
-- *************************

gui:SetRootWindow(Background);


-- Setup the window hierarchy.
-- ***************************

Background:AddChild(ChatInputFrame);
ChatInputFrame:AddChild(ChatInputFrame.ChatInput);
ChatInputFrame:AddChild(ChatInputFrame.ButtonCancel);


-- Initialization of the window contents ("constructor code").
-- ***********************************************************

function Background:OnInit()
    self:set("rect", 0, 100, 640, 50);

    local c2 = gui:new("ComponentImageT")
    c2:set("Material", "")
    c2:set("Color", 0, 0, 0)
    c2:set("Alpha", 0.7)

    self:AddComponent(c2)

    gui:activate      (false);
    gui:setInteractive(true);
    gui:showMouse     (true);
    gui:setFocus      (ChatInputFrame.ChatInput);
end

function ChatInputFrame:OnInit()
    self:set("rect", 5, 5, 630, 40);

    local c2 = gui:new("ComponentTextT")
    c2:set("Name", "Fonts/Arial")
    c2:set("Text", " Say:")
    c2:set("Scale", 0.25)
    c2:set("Padding", 0, 0)
    c2:set("Color", 0.02, 0.36, 1)
    c2:set("Alpha", 1)
    c2:set("hor. Align", -1)
    c2:set("ver. Align", 0)

    local c3 = gui:new("ComponentBorderT")
    c3:set("Width", 0.7)
    c3:set("Color", 0.7, 0.8, 0.9)
    c3:set("Alpha", 1)

    local c4 = gui:new("ComponentImageT")
    c4:set("Material", "")
    c4:set("Color", 0, 0.25, 0.7)
    c4:set("Alpha", 0.4)

    self:AddComponent(c2, c3, c4)
end

function ChatInputFrame.ChatInput:OnInit()
    self:set("rect", 30, 10, 520, 20);

    local c2 = gui:new("ComponentBorderT")
    c2:set("Width", 0.5)
    c2:set("Color", 0.7, 0.8, 0.9)
    c2:set("Alpha", 1)

    local c3 = gui:new("ComponentTextT")
    c3:set("Name", "Fonts/Arial")
    c3:set("Text", "")
    c3:set("Scale", 0.25)
    c3:set("Padding", 0, 0)
    c3:set("Color", 0.51, 0.8, 1)
    c3:set("Alpha", 1)
    c3:set("hor. Align", -1)
    c3:set("ver. Align", 0)

    local c4 = gui:new("ComponentTextEditT")
    c4:set("CursorPos", 0)
    c4:set("CursorType", 0)
    c4:set("CursorRate", 1)
    c4:set("CursorColor", 0.02, 0.36, 1)
    c4:set("CursorAlpha", 1)

    self:AddComponent(c2, c3, c4)
end

function ChatInputFrame.ButtonCancel:OnInit()
    self:set("rect", 560, 10, 60, 20);

    local c2 = gui:new("ComponentTextT")
    c2:set("Name", "Fonts/Arial")
    c2:set("Text", "Cancel")
    c2:set("Scale", 0.25)
    c2:set("Padding", 0, 0)
    c2:set("Color", 0.02, 0.36, 1)
    c2:set("Alpha", 1)
    c2:set("hor. Align", 0)
    c2:set("ver. Align", 0)

    local c3 = gui:new("ComponentBorderT")
    c3:set("Width", 0.5)
    c3:set("Color", 0.7, 0.8, 0.9)
    c3:set("Alpha", 1)

    local c4 = gui:new("ComponentImageT")
    c4:set("Material", "")
    c4:set("Color", 0, 0.25, 0.7)
    c4:set("Alpha", 0.5)

    self:AddComponent(c2, c3, c4)
end

